import { createApp } from "vue";
import App from "./App.vue";
import "./assets/global.css";

createApp(App).mount("#app");
// the root component is "App" and we're mounting it to "#app", an element in the DOM
